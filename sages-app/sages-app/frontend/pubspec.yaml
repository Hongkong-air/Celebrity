name: sages_app
description: "人类群星闪耀时 - AI 驱动的古人对话应用"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.2.2
  shared_preferences: ^2.3.3
  provider: ^6.1.2
  flutter_markdown: ^0.7.4+1
  # 音频播放（豆包 TTS 需要）
  audioplayers: ^6.1.0
  # 音频分析（嘴型同步需要）
  # flutter_sound: ^9.16.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
